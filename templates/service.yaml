apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudflare-ddns.fullname" . }}
  labels:
    app: {{ include "cloudflare-ddns.name" . }}
  spec:
    type: {{ .Values.service.type }}
# I dont believe the below is needed.  I had grabbed some defaults I saw in the helm howto page, but this doesn't ports or targetport
#   ports:
#     - port: {{ .Values.service.port }}
#       targetPort: {{ .Values.service.targetPort }}
  selector:
    app: {{ include "cloudflare-ddns.name" . }}